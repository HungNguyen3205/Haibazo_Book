version: '3.8'
services:
  db:
    image: postgres
    container_name: haibazo_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: haibazo_book_db
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data